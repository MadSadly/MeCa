# 비우면 package.json proxy로 /api → Flask(세션 쿠키 유지, 권장).
# 직접 5000 포트로 붙이면 세션 쿠키가 안 맞을 수 있음.
# LAN에서 다른 PC가 접속할 때 서버 IP로 API만 쓰려면(세션 비권장): 예) http://192.168.0.9:5000
# REACT_APP_API_URL=http://192.168.0.9:5000
REACT_APP_API_URL=
